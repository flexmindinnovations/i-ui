import { provideIcons } from '@ng-icons/core';
import { featherLoader, featherSettings, featherCheck } from '@ng-icons/feather-icons';
import { heroUsers } from '@ng-icons/heroicons/outline';
import { lucideSearch } from '@ng-icons/lucide';
// Add more icons from other sets as needed

export const IUiIconsProvider = provideIcons({
  featherLoader,
  featherSettings,
  featherCheck,
  heroUsers,
  lucideSearch,
  // ...add more icons here
});
